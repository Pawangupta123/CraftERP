import { ComingSoon } from '@/components/coming-soon'

export const metadata = { title: 'Items (SKU) · CraftERP' }

export default function SkusPage() {
  return <ComingSoon title="Items (SKU)" phase="Phase 1" />
}
